stages:
- package

Arch:
  stage: package
  image: base/archlinux
  script:
  - useradd ci
  - chown -R ci *
  - pacman -Sy --noconfirm sudo binutils fakeroot pulseaudio gnuplot
  - sudo -u ci makepkg
  artifacts:
    paths:
    - multiprocdown-*.tar.xz
