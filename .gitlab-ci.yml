stages:
- package

Arch:
  stage: package
  image: base/archlinux
  script:
  - useradd ci
  - chown -R ci *
  - pacman -Sy --noconfirm archlinux-keyring
  - pacman -S --noconfirm sudo binutils fakeroot cmake
  - sudo -u ci makepkg
  artifacts:
    paths:
    - multiprocdown-*.tar.xz
