stages:
- package

Arch:
  stage: package
  image: base/archlinux
  script:
  - useradd ci
  - chown -R ci *
  - pacman -S --noconfirm sudo
  - sudo -u ci makepkg
  artifacts:
    paths:
    - multiprocdown-*.tar.xz
